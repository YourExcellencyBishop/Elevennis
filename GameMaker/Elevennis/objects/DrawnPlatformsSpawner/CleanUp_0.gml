if (surface_exists(draw_surfaces[0])) surface_free(draw_surfaces[0]);
if (surface_exists(draw_surfaces[1])) surface_free(draw_surfaces[1]);
if (surface_exists(draw_surfaces[2])) surface_free(draw_surfaces[2]);
if (surface_exists(draw_surfaces[3])) surface_free(draw_surfaces[3]);
if (surface_exists(draw_surfaces[4])) surface_free(draw_surfaces[4]);
if (surface_exists(draw_area)) surface_free(draw_area);